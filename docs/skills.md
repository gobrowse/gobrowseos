# Skills

Skills expose only name and short description until loaded. Every modification creates a revision with author, reason, source conversations, evaluation, and promotion state. The default creation/improvement policy is `propose`.

Automatic promotion requires deterministic checks and non-regression in measured success, errors, and user corrections. LLM preference alone is not evidence. Production Skills are never silently overwritten and can roll back to a previous promoted revision.
