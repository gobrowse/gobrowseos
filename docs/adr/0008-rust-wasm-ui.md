# ADR 0008: Rust/WASM Operator Interface

Status: Accepted

Use Leptos CSR with a typed JSON/WebSocket contract. Axum serves the static output and remains independent of frontend internals. The Index Deck design uses semantic HTML, CSS Grid, URL-addressable state and native browser primitives. JavaScript is reserved for mature terminal/browser components that cannot be reasonably implemented through WASM APIs.
